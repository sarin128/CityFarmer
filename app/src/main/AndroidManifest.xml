<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yaesl.cityfarmer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_start"
        android:label="도시농부"
        android:roundIcon="@mipmap/ic_launcher_start_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".KitchenGarden_Info" />
        <activity android:name=".MyKitchenGarden" />
        <activity android:name=".Contract"></activity>
    </application>

</manifest>